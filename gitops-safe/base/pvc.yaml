apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc-3
  namespace: application
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: microk8s-hostpath
  resources:
    requests:
      storage: 2Gi
